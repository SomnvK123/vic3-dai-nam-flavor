ROC = {
	use_culture_states = yes
	is_major_formation = yes
	unification_play = dp_unify_republic_of_china
	leadership_play = dp_leadership_republic_of_china
	required_states_fraction = 0.7
	ai_will_do = {
		always = yes
	}
	possible = {
		custom_tooltip = will_lose_some_events_tt
		is_subject = no
		NOT = {
			country_tier = hegemony
		}
		NOT = {
			exists = c:ROC
		}
		NOT = {
			c:QAJ ?= this
		}
		NOT = {
			c:SOV ?= this
		}
		NOT = {
			c:TRU ?= this
		}
		NOT = {
			c:NZI ?= this
		}
		NOT = {
			c:KHO ?= this
		}
		NOT = {
			c:GER ?= this
		}
		NOT = {
			c:AUS ?= this
		}
		NOT = {
			c:HRE ?= this
		}
		NOT = {
			c:TUR ?= this
		}
		has_technology_researched = pan-nationalism
		is_country_type = recognized
		OR = {
			is_player = yes
			AND = {
				#game_date >= 1912.2.12
				custom_tooltip = {
					text = sino_event.134.a
					has_variable = warlord_state
				}
			}
		}
		OR = {
			country_has_primary_culture = cu:han
			country_has_primary_culture = cu:manchu
			country_has_primary_culture = cu:mongol
			country_has_primary_culture = cu:uighur
			country_has_primary_culture = cu:tibetan
		}
		OR = {
			has_law_or_variant = law_type:law_multicultural
			NOT = {
				has_law_or_variant = law_type:law_monarchy
				has_law_or_variant = law_type:law_theocracy
			}
		}
	}
}

#有法令，任何非大和文化人口在某地区是本土,变量>=4
IJdcr_eve_trigger_1 = {
	has_variable = on_IJdcr_integration
	any_scope_state = {
		any_scope_culture = {
			has_homeland = prev
			NOT = {
				is_primary_culture_of = cu:japanese
			}
		}
	}
	var:IJdcr_eve_var >= 4
}

#有法令，任何非大和文化人口在某地区是本土
IJdcr_eve_trigger_2 = {
	has_variable = on_IJdcr_integration
	any_scope_state = {
		any_scope_culture = {
			has_homeland = prev
			NOT = {
				is_primary_culture_of = cu:japanese
			}
		}
	}
}

#有法令，任何非大和文化人口在某地区是本土,有大学建筑
IJdcr_eve_trigger_3 = {
	has_variable = on_IJdcr_integration
	any_scope_state = {
		any_scope_culture = {
			has_homeland = prev
			NOT = {
				is_primary_culture_of = cu:japanese
			}
		}
		any_scope_building = {
			is_building_type = building_university
		}
	}
}

#有法令，任何非大和文化人口在某地区是本土,有城市建筑
IJdcr_eve_trigger_4 = {
	has_variable = on_IJdcr_integration
	any_scope_state = {
		any_scope_culture = {
			has_homeland = prev
			NOT = {
				is_primary_culture_of = cu:japanese
			}
		}
		any_scope_building = {
			OR = {
				is_building_type = building_food_industry
				is_building_type = building_textile_mill
				is_building_type = building_furniture_manufactory
				is_building_type = building_glassworks
				is_building_type = building_tooling_workshop
				is_building_type = building_paper_mill
				is_building_type = building_chemical_plant
				is_building_type = building_synthetics_plant
				is_building_type = building_steel_mill
				is_building_type = building_motor_industry
				is_building_type = building_shipyard
				is_building_type = building_automotive_industry
				is_building_type = building_electrics_industry
				is_building_type = building_arms_industry
				is_building_type = building_munition_plant
				is_building_type = building_power_plant
			}
		}
	}
}

#有法令，任何非大和文化人口在某地区是本土,有政府部门建筑
IJdcr_eve_trigger_5 = {
	has_variable = on_IJdcr_integration
	any_scope_state = {
		any_scope_culture = {
			has_homeland = prev
			NOT = {
				is_primary_culture_of = cu:japanese
			}
		}
		any_scope_building = {
			is_building_type = building_government_administration
		}
	}
}

#有法令，任何非大和文化人口在某地区是本土,有城镇中心建筑
IJdcr_eve_trigger_6 = {
	has_variable = on_IJdcr_integration
	any_scope_state = {
		any_scope_culture = {
			has_homeland = prev
			NOT = {
				is_primary_culture_of = cu:japanese
			}
		}
		any_scope_building = {
			is_building_type = building_urban_center
		}
	}
}

#
IJdcr_eve_trigger_7 = {
	has_variable = on_IJdcr_integration
	any_scope_state = {
		any_scope_culture = {
			has_homeland = prev
			NOT = {
				is_primary_culture_of = cu:japanese
			}
		}
		any_scope_building = {
			is_building_type = building_urban_center
		}
	}
}

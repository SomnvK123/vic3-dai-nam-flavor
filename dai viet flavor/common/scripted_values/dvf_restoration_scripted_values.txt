# Đếm số trụ cột (trong 6 trụ cột je_dvf_hong_bao_restoration) đã hoàn thành,
# dùng để kích hoạt phần thưởng theo mốc (Sơ/Trung Canh Tân) trước khi
# người chơi hoàn thành toàn bộ 6 trụ cột.
dvf_pillars_completed_count = {
	value = 0
	if = { limit = { dvf_pillar_1_done = yes } add = 1 }
	if = { limit = { dvf_pillar_2_done = yes } add = 1 }
	if = { limit = { dvf_pillar_3_done = yes } add = 1 }
	if = { limit = { dvf_pillar_4_done = yes } add = 1 }
	if = { limit = { dvf_pillar_5_done = yes } add = 1 }
	if = { limit = { dvf_pillar_6_done = yes } add = 1 }
}

alsace_lorraine_mining_development = {#阿尔萨斯-洛林的矿业开发
	icon = "gfx/interface/icons/invention_icons/prospecting_tech.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_ALSACE_LORRAINE
        owns_entire_state_region = STATE_LORRAINE
	}

	possible = {
		in_default = no
		has_technology_researched = watertube_boiler
		has_technology_researched = railways
		any_scope_state = {
			state_region = s:STATE_ALSACE_LORRAINE
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_LORRAINE
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_ALSACE_LORRAINE
					state_region = s:STATE_LORRAINE
				}
			}
			if = {
				limit = { has_modifier = alsace_lorraine_mining }
				remove_modifier = alsace_lorraine_mining
			}
			add_modifier = alsace_lorraine_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_ALSACE_LORRAINE
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 50
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_LORRAINE
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 50
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_ALSACE_LORRAINE
					state_region = s:STATE_LORRAINE
				}
			}
			add_modifier = {
				name = alsace_lorraine_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = alsace_lorraine_mining }
				remove_modifier = alsace_lorraine_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_ALSACE_LORRAINE
			owns_entire_state_region = STATE_LORRAINE
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = alsace_lorraine_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

ural_mining_development = {#乌拉尔的矿业开发
	icon = "gfx/interface/icons/invention_icons/prospecting_tech.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_URAL
        owns_entire_state_region = STATE_PERM
        owns_entire_state_region = STATE_CHELYABINSK
        owns_entire_state_region = STATE_UFA
	}

	possible = {
		in_default = no
		has_technology_researched = watertube_boiler
		has_technology_researched = nitroglycerin
		has_technology_researched = railways
		any_scope_state = {
			state_region = s:STATE_URAL
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_PERM
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_CHELYABINSK
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_UFA
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_URAL
					state_region = s:STATE_PERM
					state_region = s:STATE_CHELYABINSK
					state_region = s:STATE_UFA
				}
			}
			if = {
				limit = { has_modifier = ural_mining }
				remove_modifier = ural_mining
			}
			add_modifier = ural_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_URAL
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_PERM
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_CHELYABINSK
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 20
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 20
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_UFA
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 20
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_lead_mine
				level >= 20
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_URAL
					state_region = s:STATE_PERM
					state_region = s:STATE_CHELYABINSK
					state_region = s:STATE_UFA
				}
			}
			add_modifier = {
				name = ural_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = ural_mining }
				remove_modifier = ural_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_URAL
        	owns_entire_state_region = STATE_PERM
        	owns_entire_state_region = STATE_CHELYABINSK
       	 	owns_entire_state_region = STATE_UFA
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = ural_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

ruhr_mining_development = {#开发鲁尔区
	icon = "gfx/interface/icons/invention_icons/prospecting_tech.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_RUHR
        owns_entire_state_region = STATE_NORTH_RHINE
        has_state_in_state_region = STATE_RHINELAND
	}

	possible = {
		in_default = no
		has_technology_researched = watertube_boiler
		any_scope_state = {
			state_region = s:STATE_RUHR
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_NORTH_RHINE
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_RHINELAND
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_RUHR
					state_region = s:STATE_NORTH_RHINE
					state_region = s:STATE_RHINELAND
				}
			}
			if = {
				limit = { has_modifier = ruhr_mining }
				remove_modifier = ruhr_mining
			}
			add_modifier = ruhr_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_RUHR
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 25
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_NORTH_RHINE
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_RHINELAND
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_RUHR
					state_region = s:STATE_NORTH_RHINE
					state_region = s:STATE_RHINELAND
				}
			}
			add_modifier = {
				name = ruhr_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = ruhr_mining }
				remove_modifier = ruhr_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_RUHR
			owns_entire_state_region = STATE_NORTH_RHINE
			has_state_in_state_region = STATE_RHINELAND
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = ruhr_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

english_mining_development = {#英格兰矿业开发
	icon = "gfx/interface/icons/invention_icons/prospecting_tech.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_MIDLANDS
        owns_entire_state_region = STATE_YORKSHIRE
        owns_entire_state_region = STATE_LANCASHIRE
        owns_entire_state_region = STATE_WALES
	}

	possible = {
		in_default = no
		has_technology_researched = watertube_boiler
		any_scope_state = {
			state_region = s:STATE_MIDLANDS
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_YORKSHIRE
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_LANCASHIRE
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_WALES
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_MIDLANDS
					state_region = s:STATE_YORKSHIRE
					state_region = s:STATE_LANCASHIRE
					state_region = s:STATE_WALES
				}
			}
			if = {
				limit = { has_modifier = english_mining }
				remove_modifier = english_mining
			}
			add_modifier = english_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_MIDLANDS
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 25
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_YORKSHIRE
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_LANCASHIRE
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 20
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 25
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_WALES
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_MIDLANDS
					state_region = s:STATE_YORKSHIRE
					state_region = s:STATE_LANCASHIRE
					state_region = s:STATE_WALES
				}
			}
			add_modifier = {
				name = english_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = english_mining }
				remove_modifier = english_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_MIDLANDS
			owns_entire_state_region = STATE_YORKSHIRE
			owns_entire_state_region = STATE_LANCASHIRE
			owns_entire_state_region = STATE_WALES
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = english_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

biscay_mining_development = {#开采巴斯克
	icon = "gfx/interface/icons/invention_icons/prospecting_tech.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_BASQUE_COUNTRY
        owns_entire_state_region = STATE_OLD_CASTILE
        owns_entire_state_region = STATE_ASTURIAS
	}

	possible = {
		in_default = no
		has_technology_researched = watertube_boiler
		any_scope_state = {
			state_region = s:STATE_BASQUE_COUNTRY
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_OLD_CASTILE
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_ASTURIAS
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_BASQUE_COUNTRY
					state_region = s:STATE_OLD_CASTILE
					state_region = s:STATE_ASTURIAS
				}
			}
			if = {
				limit = { has_modifier = biscay_mining }
				remove_modifier = biscay_mining
			}
			add_modifier = biscay_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_BASQUE_COUNTRY
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 30
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_OLD_CASTILE
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 25
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 20
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_ASTURIAS
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 50
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_BASQUE_COUNTRY
					state_region = s:STATE_OLD_CASTILE
					state_region = s:STATE_ASTURIAS
				}
			}
			add_modifier = {
				name = biscay_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = biscay_mining }
				remove_modifier = biscay_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_BASQUE_COUNTRY
			owns_entire_state_region = STATE_OLD_CASTILE
			owns_entire_state_region = STATE_ASTURIAS
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = biscay_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

kiruna_mining_development = {#基律纳铁矿开发
	icon = "gfx/interface/icons/goods_icons/iron.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = watertube_boiler
        owns_entire_state_region = STATE_NORRLAND
	}

	possible = {
		in_default = no
		has_technology_researched = steel_railway_cars
		any_scope_state = {
			state_region = s:STATE_NORRLAND
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				state_region = s:STATE_NORRLAND
			}
			if = {
				limit = { has_modifier = kiruna_mining }
				remove_modifier = kiruna_mining
			}
			add_modifier = kiruna_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_NORRLAND
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 50
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				state_region = s:STATE_NORRLAND
			}
			add_modifier = {
				name = kiruna_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = kiruna_mining }
				remove_modifier = kiruna_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_NORRLAND
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = kiruna_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

sicily_mining_development = {#西西里硫矿开发
	icon = "gfx/interface/icons/goods_icons/sulfur.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_SICILY
	}

	possible = {
		in_default = no
		has_technology_researched = nitroglycerin
		any_scope_state = {
			state_region = s:STATE_SICILY
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				state_region = s:STATE_SICILY
			}
			if = {
				limit = { has_modifier = sicily_mining }
				remove_modifier = sicily_mining
			}
			add_modifier = sicily_mining
		}
		g:sulfur = {
			save_scope_as = sulfur_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_SICILY
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_sulfur_mine
				level >= 75
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				state_region = s:STATE_SICILY
			}
			add_modifier = {
				name = sicily_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = sicily_mining }
				remove_modifier = sicily_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_SICILY
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = sicily_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

donbas_mining_development = {#开采顿巴斯
	icon = "gfx/interface/icons/invention_icons/prospecting_tech.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_KHARKOV
        owns_entire_state_region = STATE_KURSK
	}

	possible = {
		in_default = no
		NOT = { has_law_or_variant = law_type:law_serfdom }
		has_technology_researched = watertube_boiler
		any_scope_state = {
			state_region = s:STATE_KHARKOV
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_KURSK
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_KHARKOV
					state_region = s:STATE_KURSK
				}
			}
			if = {
				limit = { has_modifier = donbas_mining }
				remove_modifier = donbas_mining
			}
			add_modifier = donbas_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_KHARKOV
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 30
				occupancy >= 0.75
			}
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 50
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_KURSK
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 50
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_KHARKOV
					state_region = s:STATE_KURSK
				}
			}
			add_modifier = {
				name = donbas_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = donbas_mining }
				remove_modifier = donbas_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_KHARKOV
			owns_entire_state_region = STATE_KURSK
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = donbas_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

wyoming_mining_development = {#怀俄明新煤业
	icon = "gfx/interface/icons/goods_icons/coal.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = watertube_boiler
		has_technology_researched = nitroglycerin
        owns_entire_state_region = STATE_WYOMING
	}

	possible = {
		in_default = no
		custom_tooltip = {
		text = finish_je_establish_national_academy_tt
		has_variable = this_country_has_national_academy
		}
		has_technology_researched = dynamite
		has_technology_researched = steel_railway_cars
		any_scope_state = {
			state_region = s:STATE_WYOMING
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				state_region = s:STATE_WYOMING
			}
			if = {
				limit = { has_modifier = wyoming_mining }
				remove_modifier = wyoming_mining
			}
			add_modifier = wyoming_mining
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_WYOMING
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 250
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				state_region = s:STATE_WYOMING
			}
			add_modifier = {
				name = wyoming_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = wyoming_mining }
				remove_modifier = wyoming_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_WYOMING
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = wyoming_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

pilbara_mining_development = {#开发皮尔巴拉
	icon = "gfx/interface/icons/goods_icons/iron.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = watertube_boiler
		has_technology_researched = nitroglycerin
        owns_entire_state_region = STATE_WESTERN_AUSTRALIA
	}

	possible = {
		in_default = no
		has_technology_researched = dynamite
		has_technology_researched = steel_railway_cars
		has_technology_researched = floating_harbor
		custom_tooltip = {
		text = finish_je_establish_national_academy_tt
		has_variable = this_country_has_national_academy
		}
		any_scope_state = {
			state_region = s:STATE_WESTERN_AUSTRALIA
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				state_region = s:STATE_WESTERN_AUSTRALIA
			}
			if = {
				limit = { has_modifier = pilbara_mining }
				remove_modifier = pilbara_mining
			}
			add_modifier = pilbara_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_WESTERN_AUSTRALIA
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 300
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				state_region = s:STATE_WESTERN_AUSTRALIA
			}
			add_modifier = {
				name = pilbara_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = pilbara_mining }
				remove_modifier = pilbara_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_WESTERN_AUSTRALIA
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = pilbara_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

ironquadrangle_mining_development = {#新铁四边形
	icon = "gfx/interface/icons/goods_icons/iron.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = watertube_boiler
		has_technology_researched = nitroglycerin
        owns_entire_state_region = STATE_MINAS_GERAIS
		owns_entire_state_region = STATE_BAHIA
	}

	possible = {
		in_default = no
		has_technology_researched = dynamite
		has_technology_researched = steel_railway_cars
		custom_tooltip = {
		text = finish_je_establish_national_academy_tt
		has_variable = this_country_has_national_academy
		}
		any_scope_state = {
			state_region = s:STATE_MINAS_GERAIS
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_BAHIA
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_MINAS_GERAIS
					state_region = s:STATE_BAHIA
				}
			}
			if = {
				limit = { has_modifier = ironquadrangle_mining }
				remove_modifier = ironquadrangle_mining
			}
			add_modifier = ironquadrangle_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_MINAS_GERAIS
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 200
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_BAHIA
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 100
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_MINAS_GERAIS
					state_region = s:STATE_BAHIA
				}
			}
			add_modifier = {
				name = ironquadrangle_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = ironquadrangle_mining }
				remove_modifier = ironquadrangle_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_MINAS_GERAIS
			owns_entire_state_region = STATE_BAHIA
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = ironquadrangle_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

mesabi_mining_development = {#苏必利尔铁矿开发
	icon = "gfx/interface/icons/goods_icons/iron.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = railways
        owns_entire_state_region = STATE_MICHIGAN
		owns_entire_state_region = STATE_WISCONSIN
		has_state_in_state_region = STATE_MINNESOTA
	}

	possible = {
		in_default = no
		has_technology_researched = nitroglycerin
		has_technology_researched = watertube_boiler
		any_scope_state = {
			state_region = s:STATE_MICHIGAN
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_WISCONSIN
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_MINNESOTA
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_MICHIGAN
					state_region = s:STATE_WISCONSIN
					state_region = s:STATE_MINNESOTA
				}
			}
			if = {
				limit = { has_modifier = mesabi_mining }
				remove_modifier = mesabi_mining
			}
			add_modifier = mesabi_mining
		}
		g:iron = {
			save_scope_as = iron_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_MICHIGAN
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 50
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_WISCONSIN
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 50
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_MINNESOTA
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_iron_mine
				level >= 100
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_MICHIGAN
					state_region = s:STATE_WISCONSIN
					state_region = s:STATE_MINNESOTA
				}
			}
			add_modifier = {
				name = mesabi_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = mesabi_mining }
				remove_modifier = mesabi_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_MICHIGAN
			owns_entire_state_region = STATE_WISCONSIN
			owns_entire_state_region = STATE_MINNESOTA
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = mesabi_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

serbia_mining_development = {#塞尔维亚煤
	icon = "gfx/interface/icons/goods_icons/coal.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = railways
		has_technology_researched = nitroglycerin
		OR = {
			AND = {
				owns_entire_state_region = STATE_WESTERN_SERBIA
				owns_entire_state_region = STATE_EASTERN_SERBIA
			}
			AND = {
				owns_entire_state_region = STATE_DELVIDEK
				owns_entire_state_region = STATE_BANAT
			}
		}
	}

	possible = {
		in_default = no
		has_technology_researched = dynamite
		custom_tooltip = {
		text = finish_je_establish_national_academy_tt
		has_variable = this_country_has_national_academy
		}
		any_scope_state = {
			state_region = s:STATE_WESTERN_SERBIA
			is_incorporated = yes
		}
		any_scope_state = {
			state_region = s:STATE_EASTERN_SERBIA
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_WESTERN_SERBIA
					state_region = s:STATE_EASTERN_SERBIA
				}
			}
			if = {
				limit = { has_modifier = serbia_mining }
				remove_modifier = serbia_mining
			}
			add_modifier = serbia_mining
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_WESTERN_SERBIA
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 50
				occupancy >= 0.75
			}
		}
		any_scope_state = {
			state_region = s:STATE_EASTERN_SERBIA
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 50
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				OR = {
					state_region = s:STATE_WESTERN_SERBIA
					state_region = s:STATE_EASTERN_SERBIA
				}
			}
			add_modifier = {
				name = serbia_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = serbia_mining }
				remove_modifier = serbia_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_WESTERN_SERBIA
			owns_entire_state_region = STATE_EASTERN_SERBIA
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = serbia_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}

shanxi_mining_development = {#山西煤业开发
	icon = "gfx/interface/icons/goods_icons/coal.dds"

	group = je_group_resource_extraction

	is_shown_when_inactive = {
		has_technology_researched = atmospheric_engine
        owns_entire_state_region = STATE_SHANXI
	}

	possible = {
		in_default = no
		has_technology_researched = nitroglycerin
		has_technology_researched = watertube_boiler
		any_scope_state = {
			state_region = s:STATE_SHANXI
			is_incorporated = yes
		}
	}

	immediate = {
		 every_scope_state = {
			limit = {
				state_region = s:STATE_SHANXI
			}
			if = {
				limit = { has_modifier = shanxi_mining }
				remove_modifier = shanxi_mining
			}
			add_modifier = shanxi_mining
		}
		g:coal = {
			save_scope_as = coal_scope
		}
	}

	complete = {
		any_scope_state = {
			state_region = s:STATE_SHANXI
			is_incorporated = yes
			any_scope_building = {
				is_building_type = building_coal_mine
				level >= 100
				occupancy >= 0.75
			}
		}
	}

	on_complete = {
		every_scope_state = {
			limit = {
				state_region = s:STATE_SHANXI
			}
			add_modifier = {
				name = shanxi_mining_community
				months = -1
			}
			if = {
				limit = { has_modifier = shanxi_mining }
				remove_modifier = shanxi_mining
			}
		}
	}

	fail = {
		NOT = {
			owns_entire_state_region = STATE_SHANXI
		}
	}

	status_desc = {
		triggered_desc = { 
			desc = shanxi_mining_development_status
		}
	}

	weight = 100

	should_be_pinned_by_default_uninvolved_or_context = yes
}
beijing_and_state_var_progress_bar = {#地方自治
	name = "beijing_and_state_var_progress_bar"
	desc = "beijing_and_state_var_progress_bar_desc"

	default_bad = yes

	monthly_progress = {
		if = {
 			limit = {
 				has_variable = beijing_and_state_first_action_var
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_1_progress"
			   	value = 4
 			}
 		}
 		if = {
 			limit = {
 				has_variable = beijing_and_state_second_action_var
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_2_progress"
			   	value = 6
 			}
 		}
 		if = {
 			limit = {
 				has_variable = beijing_and_state_third_action_var
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_3_progress"
			   	value = 2
 			}
 		}
 		if = {
 			limit = {
 				any_scope_state = {
					is_incorporated = yes
					tax_capacity < tax_capacity_usage
				}
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_4_progress"
			   	value = 1
 			}
 		}
 		if = {
 			limit = {
 				legitimacy < 50
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_5_progress"
			   	value = 1
 			}
 		}
 		if = {
 			limit = {
 				authority < -500
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_6_progress"
			   	value = 1
 			}
 		}
 		if = {
 			limit = {
 				has_modifier = opium_wars_lost
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_7_progress"
			   	value = 1
 			}
 		}
 		if = {
 			limit = {
 				has_technology_researched = central_archives
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_8_progress"
			   	value = -1
 			}
 		}
 		if = {
 			limit = {
 				has_technology_researched = central_planning
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_9_progress"
			   	value = -1
 			}
 		}
 		if = {
 			limit = {
 				legitimacy >= 75
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_10_progress"
			   	value = -1
 			}
 		}
 		if = {
 			limit = {
 				authority >= 300
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_11_progress"
			   	value = -1
 			}
 		}
 		if = {
 			limit = {
 				authority >= 500
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_12_progress"
			   	value = -1
 			}
 		}
 		if = {
 			limit = {
 				has_variable = beijing_and_state_forth_action_var
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_13_progress"
			   	value = -2
 			}
 		}
 		if = {
 			limit = {
 				ig:ig_landowners = {
 					is_in_government = yes
 					ig_approval >= 5
 				}
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_14_progress"
			   	value = -1
 			}
 		}
 		if = {
 			limit = {
 				legitimacy >= 90
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_15_progress"
			   	value = -1
 			}
 		}
 		if = {
 			limit = {
 				legitimacy >= 75
 				OR = {
 					capital.state_region = s:STATE_BEIJING
 					capital.state_region = s:STATE_NANJING
 				}
 			}
 			add = {
 				desc = "beijing_and_state_var_progress_bar_16_progress"
			   	value = -1
 			}
 		}
 	}

	start_value = 180
	min_value = 0
	max_value = 1000
}
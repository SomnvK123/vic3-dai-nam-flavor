french_provisional_republic_republic_progress_bar = {#“昨日共和党人”
	name = "french_provisional_republic_republic_progress_bar"
	desc = "french_provisional_republic_republic_progress_bar_desc"

	default_bad = yes

	monthly_progress = {
		if = {
 			limit = {
 				has_variable = french_provisional_republic_1_button_selected
 			}
 			add = {
 				desc = "french_provisional_republic_republic_progress_bar_change_button"
			   	value = 2
 			}
 		}
 		if = {
 			limit = {
 				has_global_variable = french_provisional_republic_republican_event_support_var
 			}
 			add = {
 				desc = "french_provisional_republic_republic_progress_bar_change_event"
			   	value = 10
 			}
 		}
 	}

	start_value = 120
	min_value = 0
	max_value = 500
 }

 french_provisional_republic_monarchy_progress_bar = {#“明日共和党人”
	name = "french_provisional_republic_monarchy_progress_bar"
	desc = "french_provisional_republic_monarchy_progress_bar_desc"

	default_green = yes

	monthly_progress = {
		if = {
 			limit = {
 				has_variable = french_provisional_republic_2_button_selected
 			}
 			add = {
 				desc = "french_provisional_republic_monarchy_progress_bar_change_button"
			   	value = 2
 			}
 		}
 		if = {
 			limit = {
 				has_global_variable = french_provisional_republic_monarchy_event_support_var
 			}
 			add = {
 				desc = "french_provisional_republic_monarchy_progress_bar_change_event"
			   	value = 10
 			}
 		}
 	}

	start_value = 60
	min_value = 0
	max_value = 500
 }

 communaute_union_france_progress_bar = {#联邦稳定度
	name = "communaute_union_france_progress_bar"
	desc = "communaute_union_france_progress_bar_desc"

	default_bad = yes

	monthly_progress = {
		if = {
 			limit = {
 				has_variable = communaute_union_france_1_var
 			}
 			add = {
 				desc = "communaute_union_france_progress_bar_1_tt"
			   	value = -5
 			}
 		}
 		if = {
 			limit = {
 				has_variable = communaute_union_france_2_var
 			}
 			add = {
 				desc = "communaute_union_france_progress_bar_2_tt"
			   	value = 5
 			}
 		}
 		if = {
 			limit = {
 				has_variable = communaute_union_france_3_var
 			}
 			add = {
 				desc = "communaute_union_france_progress_bar_3_tt"
			   	value = 5
 			}
 		}
 		if = {
 			limit = {
 				has_variable = communaute_union_france_4_var
 			}
 			add = {
 				desc = "communaute_union_france_progress_bar_4_tt"
			   	value = -5
 			}
 		}
 		if = {
 			limit = {
 				has_variable = communaute_union_france_5_var
 			}
 			add = {
 				desc = "communaute_union_france_progress_bar_5_tt"
			   	value = 5
 			}
 		}
 	}

	start_value = 0
	min_value = 0
	max_value = 1000
 }